# Contributing

1. Create a branch from the default branch.
2. Run `npm ci`.
3. Validate changes with `npm run lint`, `npm run build`, and `npm run test -- --watch=false`.
4. Follow Conventional Commits.
